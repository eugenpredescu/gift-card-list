export const values = {
  account: 'account',
  options: [{ label: 'account', value: 'account' }],
  setAccount: jest.fn(),
  save: jest.fn(),
  clickSave: jest.fn(),
  showAlert: 0,
  modalSave: false,
  handleCloseAlert: jest.fn(),
  setModalSave: jest.fn(),
}
